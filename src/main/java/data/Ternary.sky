# Define two numbers
define a := 15;
define b := 10;

# Calculate minimum using ternary operator
define min := (a < b) ? a : b;

# Calculate maximum using ternary operator
define max := (a > b) ? a : b;

# Display the results
show_nl("Minimum:", min);
show_nl("Maximum:", max);
